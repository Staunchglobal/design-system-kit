/**
 * Opt-in feature-flags-admin entry — filesystem discovery picks up slug
 * `feature-flags-admin`. Prefer importing from
 * `@/components/feature-flags-admin/*` directly.
 */
export { FeatureFlagMatrix } from '@/components/feature-flags-admin/feature-flag-matrix'
